---
import { tv } from "tailwind-variants";

interface Props {
    className?: string;
}

const { className = "" } = Astro.props;

const blockquote = tv({
    base: [""],
});
---

<p class={blockquote({ class: className })}>
    <slot />
</p>
